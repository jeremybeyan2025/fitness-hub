const cart=[];const cartItems=document.getElementById('cartItems');const cartTotal=document.getElementById('cartTotal');function money(n){return '$'+n.toFixed(2)}function renderCart(){if(!cartItems)return;if(!cart.length){cartItems.innerHTML='<div class="cart-line"><span>No items yet</span><strong>$0</strong></div>';cartTotal.textContent='$0.00';return}cartItems.innerHTML=cart.map(item=>`<div class="cart-line"><span>${item.name}</span><strong>${money(item.price)}</strong></div>`).join('');cartTotal.textContent=money(cart.reduce((a,x)=>a+x.price,0))}document.querySelectorAll('[data-product]').forEach(btn=>btn.addEventListener('click',()=>{cart.push({name:btn.dataset.product,price:Number(btn.dataset.price)});renderCart()}));renderCart();